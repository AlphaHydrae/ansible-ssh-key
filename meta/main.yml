galaxy_info:
  author: Simon Oulevay (Alpha Hydrae)
  description: Generates an SSH key for a user.
  license: MIT
  min_ansible_version: 2.0

  platforms:
    - name: Darwin
      versions:
        - all
    - name: Fedora
      versions:
        - all
    - name: Ubuntu
      versions:
        - all

  galaxy_tags:
    - ssh
    - sshkey
    - sshkeygen

dependencies:
  - role: AlphaHydrae.multipass
